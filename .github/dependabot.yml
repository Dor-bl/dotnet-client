version: 2
updates:
  - package-ecosystem: "nuget"
    # Files stored in src/ppium.Net directory
    directory: "/src/Appium.Net" 
    schedule:
      interval: "daily"

  - package-ecosystem: "github-actions"
    # Workflow files stored in the
    # default location of `.github/workflows`
    directory: "/"
    schedule:
      interval: "weekly"
